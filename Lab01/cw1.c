#include <stdio.h>
//
// Napisać program, który wypisuje na konsolę nr albumu studenta (osoby rozwiązującej program).
//
int main() {
    int numer_albumu = 012345; 
    printf("Numer albumu studenta: %d\n", numer_albumu);

    return 0;
}
